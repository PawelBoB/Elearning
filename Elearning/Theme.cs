using Microsoft.VisualBasic;
using System;

namespace Elearning
{
    public class Theme
    {
        private readonly string name;
        public Theme(string name)
        {
            this.name = name;
        }
    }
}